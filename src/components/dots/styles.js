import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    marginTop: 10,
    flexDirection: 'row',
    justifyContent: 'center'
  },
  dot: {
    width: 6,
    height: 6,
    borderRadius: 150,
    backgroundColor: '#fff',
    marginLeft: 10
  }
})
